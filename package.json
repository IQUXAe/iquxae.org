{
    "name": "iquxae-org",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build && esbuild src/worker.ts --bundle --outfile=dist/_worker.js --format=esm --platform=browser",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^5.0.0"
    },
    "dependencies": {
        "@cloudflare/workers-types": "^4.20260131.0",
        "esbuild": "^0.27.2",
        "hono": "^4.11.7"
    }
}